import React from "react"
import styled from "@emotion/styled"

export const Topbar = () => <TopbarContainer className="topbar"></TopbarContainer>

const TopbarContainer = styled.div`
  height: 8px;
  background-color: var(--primary);
`
